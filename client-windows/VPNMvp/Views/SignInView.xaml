<UserControl x:Class="VPNMvp.Views.SignInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="640"
             Background="Transparent">

    <Grid>
        <!-- Centered container -->
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Width="380" Padding="20" CornerRadius="10" Background="White">
                <!-- explicit DropShadowEffect (safe) -->
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="12" ShadowDepth="4" Opacity="0.18"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Sign in" FontSize="22" FontWeight="Bold" Margin="0,0,0,12" HorizontalAlignment="Center"/>

                    <TextBlock Text="Email" FontWeight="SemiBold" Margin="2,0,0,6"/>
                    <TextBox x:Name="EmailTextBox"
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                             Height="30"
                             Width="320"
                             HorizontalAlignment="Center"/>

                    <TextBlock Text="Password" FontWeight="SemiBold" Margin="2,12,0,6"/>
                    <PasswordBox x:Name="PasswordBox" Width="320" Height="30" HorizontalAlignment="Center"/>

                    <!-- Sign-in button centered under password -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                        <Button x:Name="SignInButton"
                                Content="{Binding SignInButtonText}"
                                Width="140"
                                Height="36"
                                Command="{Binding SignInCommand}"/>
                    </StackPanel>

                    <!-- Error message -->
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,12,0,0" TextWrapping="Wrap" HorizontalAlignment="Center"/>

                    <!-- Small helper text -->
                    <TextBlock Text="Use test@example.com / password123" FontSize="11" Foreground="Gray" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
