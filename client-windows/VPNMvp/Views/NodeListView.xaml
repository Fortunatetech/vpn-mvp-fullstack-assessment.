<UserControl x:Class="VPNMvp.Views.NodeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:VPNMvp.Converters"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <conv:NodeButtonLabelConverter x:Key="NodeLabelConv"/>
    </UserControl.Resources>

    <Grid Margin="12">
        <StackPanel>
            <TextBlock Text="Available VPN Nodes" FontSize="18" FontWeight="Bold" Margin="0,0,0,8" />
            <ItemsControl ItemsSource="{Binding Nodes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Padding="8" Margin="0,4,0,4">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <StackPanel Width="420">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                                    <TextBlock Text="{Binding Country}" />
                                    <TextBlock>
                                        <Run Text="Latency: " />
                                        <Run Text="{Binding LatencyMs}" />
                                        <Run Text=" ms" />
                                    </TextBlock>
                                </StackPanel>

                                <Button Width="100"
                                        Command="{Binding DataContext.ConnectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}">
                                    <Button.Content>
                                        <MultiBinding Converter="{StaticResource NodeLabelConv}">
                                            <!-- current item (Node) -->
                                            <Binding />
                                            <!-- bind to ViewModel's ConnectedNode -->
                                            <Binding Path="DataContext.ConnectedNode" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                        </MultiBinding>
                                    </Button.Content>
                                </Button>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="{Binding ConnectionStatus}" Margin="0,12,0,0" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding ConnectedNode.Name, TargetNullValue='Not connected'}" Margin="0,4,0,0" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</UserControl>
